const createMangaChapter = ( _root, { input }, _context) => {
  const {
    mangaSeriesId,
    mangaSeriesAuthor,
    mangaSeriesTitle,
    chapterNumber,
    pictures
  } = input

  const mangaSeries = do {

  }
}
